<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YumlyPlanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.YumlyPlanner" parent="Base.Theme.YumlyPlanner" />
    <style name="DialogTheme" parent="Theme.Material3.DayNight.Dialog">
        <item name="colorAccent" >@color/primaryColor</item>
        <item name="buttonBarPositiveButtonStyle">@color/primaryColor</item>
        <item name="buttonBarNegativeButtonStyle">@color/primaryColor</item>
    </style>

</resources>